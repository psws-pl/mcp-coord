import { PanelLoading } from "@/components/dashboard/panel-loading";

export default function Loading() {
  return (
    <PanelLoading
      title="Loading plans"
      description="Preparing the plans panel shell and progress summaries."
    />
  );
}
