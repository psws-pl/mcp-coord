import { PanelLoading } from "@/components/dashboard/panel-loading";

export default function Loading() {
  return (
    <PanelLoading
      title="Loading tasks"
      description="Preparing the task board shell and status summaries."
    />
  );
}
