import { PanelLoading } from "@/components/dashboard/panel-loading";

export default function Loading() {
  return (
    <PanelLoading
      title="Loading messages"
      description="Preparing the message timeline shell and delivery summaries."
    />
  );
}
