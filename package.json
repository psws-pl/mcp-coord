{
  "name": "mcp-coord",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "lint": "pnpm -r --if-present lint",
    "start": "pnpm --filter @mcp-coord/api start",
    "test": "pnpm -r --if-present test"
  }
}
